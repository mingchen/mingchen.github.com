mingchen.github.com

public key generate:

ssh-keygen -b 2048 -t ras -c "your_name@domain"

In window .ssh is located in 
  C:\Documents and Settings\<user name>\.ssh
    id_ras
    id_rsa.pub


GIT commands

  git add index.html

  git commit -m "First message"

  git push -u origin master


References:

  o CNAME support for GitHub Pages
     https://github.com/blog/315-cname-support-for-github-pages

--

END
